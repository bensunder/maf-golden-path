"""Run locally: ``python -m {{ package_name }}`` (or use DevUI for the inner loop)."""

import os

import uvicorn

if __name__ == "__main__":
    uvicorn.run("{{ package_name }}.app:app", host="0.0.0.0", port=int(os.getenv("PORT", "8000")))
